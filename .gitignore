__pycache__/
*.py[cod]

# Kernel module build output. These are regenerated by `make` against
# whichever kernel is running, so tracking them guarantees conflicts.
*.o
*.ko
*.mod
*.mod.c
.*.cmd
.tmp_versions/
modules.order
Module.symvers
*.symvers

# Built tools.
iskn_harness
trace_serial.so

# Local probe/trace captures.
probe.log
trace.log
calibration.log
calibration.decoded
